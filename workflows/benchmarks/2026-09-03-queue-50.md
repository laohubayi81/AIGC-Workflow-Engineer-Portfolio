# 数字人写真批量队列 · 50 张（Day 12）

- 任务表：`workflows/queue/jobs.csv`（50 条，`00027.jpg` × park/studio/night 轮换）
- 运行：先 `--limit 5` 冒烟（5/5），再 `--state …/20260903_174818/state.jsonl` 续跑 006–050
- 客户端：`ComfyClient.generate()` 串行；失败会再试 1 次（本轮未触发）
- 机器：本机 RTX 5080 Laptop 16GB · RAW FP8 · Depth 0.8 · myface 0.85

## 结果

| 项 | 值 |
|---|---|
| 任务 / 成功 / 失败 | **50 / 50 / 0** |
| 成功率 | **100%** |
| 场景 | park 17 · studio 17 · night 16 |
| 单张耗时（state 内 seconds） | 均值 **57.22 s** · P50 **57.3 s** · P90 **57.3 s** · min 56.5 · max 57.4 |
| GPU 有效时间合计 | **2860.9 s**（约 47.7 min） |
| 墙钟 | 冒烟 286 s + 续跑 2576.3 s ≈ **47.7 min** |
| 吞吐 | 约 **63 张/小时**（与 Day 9 单张 57.3 s 一致） |
| 本地成本（0.4 元/h） | 50 × 0.006 ≈ **0.32 元** |
| AutoDL 折算（2.18 元/h） | 50 × 0.035 ≈ **1.74 元** |

断点续跑有效：第二次启动 `skip=5`，未重出 001–005。输出前缀 `Queue_{scene}_{id}_*.png`，在 `ComfyUI-Shared/output/`。原始 `state.jsonl` / 日志不入库（`workflows/queue/runs/` 已 gitignore）。
